# Build Information Properties
# This file will be filtered by Maven to inject build-time values

# Build timestamp
build.timestamp=${build.timestamp}

# Git commit information
build.commit.sha=${build.commit.sha}
build.commit.tag=${build.commit.tag}

# Application version
build.version=${project.version}

# Build number/pipeline ID
build.number=${build.number}
